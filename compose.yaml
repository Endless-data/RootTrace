services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: roottrace
      POSTGRES_DB: roottrace
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "5433:5432"
    volumes:
      - ./sql:/schema:ro
